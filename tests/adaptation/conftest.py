"""Adaptation-specific fixtures.

Keep fixtures here when they represent adaptation semantics such as confirmed
shifts, rejected shifts, labelled evaluation streams, or classifier state.
Fixtures shared across unrelated research modules should remain in
``tests/conftest.py``.
"""
